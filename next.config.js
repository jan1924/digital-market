/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "digital-marketplace-production-cca8.up.railway.app",
    ],
  },
};

module.exports = nextConfig;
